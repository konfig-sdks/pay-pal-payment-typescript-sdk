import { PayPalPayment } from "./index";

describe("pay-pal-payment-typescript-sdk", () => {
    it("initialize client", async () => {
        const paypalpayment = new PayPalPayment({
        });
    });
});
